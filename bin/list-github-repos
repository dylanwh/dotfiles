#!/bin/sh
fd \
  --exec-batch \
    rg --no-heading -Nor '$1' \
    'url\s+=\s+(?:(?:git@)?github.com:|(?:https|git)://github.com/)([^.]+)' \
    '{}/config' \
  ';' \
  -Htd '^\.git$' \
  ~/Code | sort -u 
