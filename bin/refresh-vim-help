#!/usr/bin/env fish

for packdir in ~/.vim/pack/*
    for dir in $packdir/*
        cd $dir
        for docdir in */doc
            echo "helptags $docdir"
            vim -u NONE -c "helptags $docdir" -c q
        end
    end
end
