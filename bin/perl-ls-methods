#!/usr/bin/perl
use strict;
use warnings;

my $mod = shift;
my $pkg = shift || $mod;

eval <<PERL;
use $mod;
package $pkg;
use metaclass;
PERL

print join ("\n", map("$_->{package_name}->$_->{name}",
		$pkg->meta->get_all_methods())), "\n";


