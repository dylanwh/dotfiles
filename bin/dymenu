#!/bin/zsh

# dmenu-patchy is assumed
font='-xos4-terminus-bold-r-*-*-18-*-*-*-*-*-*-*'
args=(-fn $font -b)

if [[ -f ~/.outside ]]; then
    exec dmenu-light $args "$@"
else
    exec dmenu-dark $args "$@"
fi
