#!/usr/bin/perl
use strict;
use warnings;
use Getopt::Long qw(:config gnu_getopt);

my $name  = '';
my $exec  = '';
my $ssh   = '';
my $dtach = 0;

exec 'urxvt' => @ARGV;
GetOptions(
    'name|n=s'  => \$name,
    'exec|e=s'  => \$exec,
    'ssh|s=s'   => \$ssh,
    'dtach|d' => \$dtach,
);


