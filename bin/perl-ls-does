#!/usr/bin/env perl
use strict;
use warnings;

my $pkg = shift;
eval <<PERL;
use $pkg;
package $pkg;
use metaclass;
PERL

print join ("\n", map("$_->{class}->$_->{name}",
        $pkg->meta->compute_all_applicable_methods())), "\n";
