#!/bin/zsh

folder=$1
if [[ -z $folder ]]; then
    exit 1
fi

rm -rv ~/mail/home/$folder
rm -rv ~/.offlineimap/Repository-home/FolderValidity/$folder
rm -rv ~/.offlineimap/Repository-hardison.net/FolderValidity/$folder
rm -rv ~/.offlineimap/Account-home/LocalStatus-sqlite/$folder
