#!/usr/bin/env zsh
mkdir ~/tmp/pda
cd ~/tmp/pda
remind2pda
abook2pda
yodo2pda

echo "Hit the sync button!"
inotifywait -r -e create /dev >/dev/null 2>/dev/null
while ! pilot-xfer -i -p /dev/pilot *.pdb; do
    sleep 2
done

rm -vrf ~/tmp/pda
