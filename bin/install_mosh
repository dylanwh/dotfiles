#!/usr/bin/env fish

test -d ~/.cache/build
or mkdir -p ~/.cache/build
cd ~/.cache/build

test -d mosh
or git clone https://github.com/mobile-shell/mosh

cd mosh

./autogen.sh
and ./configure
and make -j 16
and sudo make install
and cd ~
and rm -vfr ~/.cache/build
