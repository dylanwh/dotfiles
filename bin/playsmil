#!/usr/bin/env perl

use strict;
use warnings;
use LWP::Simple qw( get );

my $url    = shift;
my @player = qw(mplayer);

my $smil = get($url) or die "Can't get $url!\n";
$smil =~ /src=['"](.+?\.r[am])["']/;
my $url_ra = $1;


exec(@player, $url_ra);

