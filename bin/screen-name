#!/usr/bin/env perl
use strict;
use warnings;

my $ppid = getppid();
my @screens = `screen -ls`;


my ($n) = grep (/$ppid/, @screens);
if ($n) {
	$n =~ /$ppid\.(\S+)/;
	print "$1\n";
} else {
	print "???\n";
}

