#!/bin/zsh

DIR=$1; shift
if [[ -z $DIR ]]; then
    exit 1
fi

rm -r ~/mail/home/$DIR
rm -r ~/.offlineimap/Account-home/LocalStatus-sqlite/$DIR
rm -r ~/.offlineimap/Repository-hardison.net/FolderValidity/$DIR
