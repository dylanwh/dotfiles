# vim: set ft=desktop foldmethod=marker cms=#%s:

[general]#{{{
accounts        = home
pythonfile      = ~/.offlineimap.py
maxconnections  = 3
maxsyncaccounts = 3
holdconnectionopen = yes

ui = Noninteractive.Basic
#}}}

[Repository hardison.net]
type         = Gmail
remoteuserval   = get_username("hardison.net")
remotepassval   = get_password("hardison.net")
folderfilter = lambda folder: folder not in '[Gmail]/All Mail'

