[push]
	default = matching

[user]
	email = dylan@hardison.net
	name  = Dylan William Hardison

[alias]
	co = checkout
	ci = commit
	br = branch
	st = status
	up = !git fetch --all --tags --prune && git pull --rebase

[color]
	ui = auto
	diff = auto
	status = auto
	branch = auto
