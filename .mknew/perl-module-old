[%# vim: set ft=template: %]
package [% module %];
use [% IF class == 'role'; 'Moose::Role'; ELSE; 'Moose'; END %];
use namespace::autoclean;

[% IF class != 'role' %]__PACKAGE__->meta->make_immutable;[% END %]

1;[%- '' -%]
