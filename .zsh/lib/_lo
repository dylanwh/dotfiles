#compdef lo
typeset -A opt_args
local context state line ignored
local layout_dir=$HOME/.screenlayout

ignored=(after last current )

_files -W $layout_dir -F ignored -g "*@$HOST.sh(:t:r:s/@$HOST/)" && return 0

return 1
