# vim: set ft=zsh:
local -a rules
local start="."

if [[ -d $1 ]]; then
	start=$1
	shift
fi

for file in $argv; do
	if (( $#rules == 0 )); then
		rules=("-name" $file);
	else
		rules=($rules '-or' '-name' $file)
	fi
done

find $start $rules
